let guestList: string[] = ["Rebecca", "Robert", "Christina", "Charlie"];

guestList.forEach(guest => {console.log("Hello " + guest + ", please join us for dinner.")});   // prints welcome for each elements in the array. Uses callback (guest => {})
 